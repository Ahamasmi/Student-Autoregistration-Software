1.Make sure you have pyhton 3 or higher installed and set in system PATH
2.Put the input in the excel sheet f1
3.Always keep the excel file in the same directory as the python files
4.In the excel sheet Date form must be yyyy/mm/dd
5.Install these pyhton packages xlrd,selenium,tkinter,openpyxl,keyboard,excel
6.If you are running on a physical server rather than xampp then go into internal.py at line 32 and replace the url with the url of the folder sih on the server
7.Run the gui.py amd enter a valid(one that exists in the excel sheet) JEE roll no and it will do the rest on its own
